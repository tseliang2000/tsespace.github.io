--- portfolio.html
+++ portfolio.html
@@ -21,6 +21,7 @@
     }
     *{box-sizing:border-box}
     html,body{height:100%}
+    html { scroll-behavior: smooth; }
     body{
       margin:0;
       font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial; 
@@ -41,10 +42,12 @@
     .menu a{
       text-decoration:none; color:var(--ink); font-size:14px; opacity:.8
     }
-    .menu a:hover{opacity:1;}
+    .menu a:hover{opacity:1; transition: opacity 0.2s ease;}
     .play{
       width:34px;height:26px;border:1px solid #ddd;border-radius:8px;
       display:grid;place-items:center;background:#fff; box-shadow:var(--shadow);
+      cursor: pointer; transition: transform 0.2s ease, box-shadow 0.2s ease;
     }
+    .play:hover { transform: translateY(-1px); box-shadow: 0 12px 34px rgba(0,0,0,.12); }
 
     /* hero panel */
@@ -75,9 +78,11 @@
     @media (max-width: 900px){ .grid{grid-template-columns:1fr} }
     .card{
       background:var(--panel); border-radius:16px; padding:22px 22px; box-shadow:var(--shadow);
+      transition: transform 0.2s ease, box-shadow 0.2s ease;
     }
+    .card:hover { transform: translateY(-2px); box-shadow: 0 12px 34px rgba(0,0,0,.12); }
     .card h3{margin:4px 0 2px; font-size:16px}
     .card .meta{color:var(--muted); font-size:13px}
     .list{display:grid; gap:12px; margin:0; padding:0; list-style:none}
@@ -88,7 +93,8 @@
     /* footer */
     footer{max-width:var(--max); margin:60px auto 50px; padding:0 20px; color:var(--muted); font-size:14px}
 
     /* links */
     .links{display:flex; flex-wrap:wrap; gap:12px; margin-top:18px}
-    .chip{border:1px solid #e5e7eb; padding:8px 12px; border-radius:999px; text-decoration:none; color:var(--link); background:#fff}
+    .chip{border:1px solid #e5e7eb; padding:8px 12px; border-radius:999px; text-decoration:none; color:var(--link); background:#fff; transition: all 0.2s ease;}
     .chip:hover{border-color:#d1d5db; background: #f9fafb; transform: translateY(-1px);}
   </style>
